spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/huajing/javaweb
          search-paths: spring-cloud/spring-config/config-repo/
      label: master